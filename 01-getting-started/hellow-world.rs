// Like C, you need a main() function as the entry point for the programme
fn main() {
    // `println!` is a MACRO. You call a macro with `!` at the end; those without `!` are function calls instead
    println!("Hello, world!"); // You NEED a semicolon for end of statement in Rust
}